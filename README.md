# mini-container-runtime
A minimal Docker-like container runtime implemented in C, demonstrating process isolation using namespaces, filesystem isolation via chroot, and resource management with cgroups.
